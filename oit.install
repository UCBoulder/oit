<?php

/**
 * @file
 * Install, update and uninstall functions for the sympa_initialize module.
 */

use Drupal\Component\Utility\Random;
use Drupal\menu_link_content\Entity\MenuLinkContent;
use Drupal\node\Entity\Node;
use Drupal\redirect\Entity\Redirect;
use Drupal\user\Entity\User;

/**
 * Helper function to update node body text.
 */
function update_node_body($nid, $body, $format) {
  $query = \Drupal::entityQuery('node')
    ->condition('nid', $nid)
    ->accessCheck(FALSE);

  $nids = $query->execute();
  foreach ($nids as $nid) {
    $node = Node::load($nid);
    $node->set('body', [
      'format' => $format,
      'value' => $body,
    ]);
    $node->save();
  }
}

/**
 * Add missing manage_webform users.
 */
function oit_update_9000() {
  $usernames = [
    'jonesdr',
    'fast',
    'gewe8216',
    'tiha2368',
    'joca3391',
    'sode2485',
    'aled9463',
    'prte0550',
    'evansc',
    'houston',
    'thba2172',
    'kero7268',
    'alla1028',
    'laba7221',
    'sisa6831',
    'amwi5704',
  ];
  foreach ($usernames as $username) {
    $language = \Drupal::languageManager()->getCurrentLanguage()->getId();
    $user = User::create();
    $random = new Random();

    // Mandatory.
    $user->setPassword($random->string());
    $user->enforceIsNew();
    $user->setEmail($random->string());
    $user->setUsername($username);

    // Optional.
    $user->set('langcode', $language);
    $user->set('preferred_langcode', $language);
    $user->set('preferred_admin_langcode', $language);
    $user->addRole('manage_webforms');
    $user->activate();

    // Save user account.
    $user->save();
  }
}

/**
 * Truncate cache_container table to remove zap modules.
 */
function oit_update_9001() {
  \Drupal::database()->truncate('cache_container')->execute();
}

/**
 * Set menu items for phising nodes.
 */
function oit_update_9002() {
  $term_id = 849;
  $query = \Drupal::database()->select('taxonomy_index', 'ti');
  $query->fields('ti', ['nid']);
  $query->condition('ti.tid', $term_id);
  $nodes = $query->execute()->fetchAll();

  foreach ($nodes as $nid) {
    $nid = $nid->nid;
    $menu = MenuLinkContent::create([
      'title' => 'Phishing Email-' . $nid,
      'link' => ['uri' => 'entity:node/' . $nid],
      'menu_name' => 'main',
      'parent' => 'views_view:views.phishing.page_1',
      'weight' => 0,
    ]);
    $menu->setUnpublished(TRUE);
    $menu->save();
  }
}

/**
 * Disable/enable servicenow to enable new schema/class.
 */
function oit_update_9003() {
  $module = ['servicenow'];
  \Drupal::service('module_installer')->uninstall($module, TRUE);
  \Drupal::service('module_installer')->install($module, TRUE);
}

/**
 * Clear out field_exclude_from_search field.
 */
function oit_update_9004() {
  \Drupal::database()->truncate('node__field_exclude_from_search')->execute();
  \Drupal::database()->truncate('node_revision__field_exclude_from_search')->execute();
}

/**
 * Trashing markdown.
 */
function oit_update_9005() {
  \Drupal::configFactory()->getEditable('filter.format.markdown')->delete();
  $module = ['markdown'];
  \Drupal::service('module_installer')->uninstall($module, TRUE);
  \Drupal::configFactory()->getEditable('markdown.settings')->delete();
}

/**
 * Implements hook_update().
 */
function oit_update_9006() {
  $config = \Drupal::configFactory()->getEditable('oit.settings');
  $config->set('show_login_form', TRUE)->save();
}

/**
 * Death to phishing nodes.
 */
function oit_update_9007() {
  $phish_cut_off = strtotime("today");
  $dotn = \Drupal::service('oit.dotn');
  $dotn->update(849, $phish_cut_off);
  // Delete taxonomy term 849, page type of phishing.
  $controller = \Drupal::entityTypeManager()->getStorage('taxonomy_term');
  $entities = $controller->loadMultiple([849]);
  $controller->delete($entities);
}

/**
 * Remove oitbik config.
 */
function oit_update_9008() {
  $connection = \Drupal::database();
  $connection->delete('config')
    ->condition('name', 'oitbik.settings')
    ->execute();
}

/**
 * Update Services page.
 */
function oit_update_10002() {
  $nid = 329;
  $format = 'rich_text';
  $body = <<<EOT
  <div class="no-list-style">
    <ul class="flex flex-start">
        <li>
            <a class="button-internal button-internal--active" href="/services">Services by Category</a>
        </li>
        <li>
            <a class="button-internal" href="/services/search">Filter Services</a>
        </li>
        <li>
            <a class="button-internal" href="/tutorial/search">Filter Tutorials</a>
        </li>
    </ul>
    <p>
        [block id="86"][/block]
    </p>
</div>
EOT;
  update_node_body($nid, $body, $format);
}

/**
 * Clean out old menus.
 */
function oit_update_10003() {
  $menu_items = [
    // md-about-oit.
    25353,
    25361,
    // md-about-oit-tp.
    25371,
    25399,
    // md-accounts-related-service.
    25355,
    25373,
    25375,
    // md-accounts-top-pages.
    25389,
    25391,
    25393,
    25397,
    // md-services-suggested.
    25309,
    25311,
    25345,
    25347,
    25385,
    // md-services-viewby.
    25313,
    // md-shware-software.
    25295,
    25297,
    25299,
    25305,
    25307,
    // md-shware-top-downloads.
    25301,
    25303,
    25319,
    // md-shware-top-pages.
    25333,
    25335,
    25339,
    25341,
    // md-top-support-training.
    25323,
    // oit-footer-avc.
    26635,
    26637,
    26639,
    26641,
    // oit-footer-resources.
    26629,
    28919,
    30899,
    36341,
    // Navigation.
    11,
    29563,
    36956,
    // tab-security-resources.
    25423,
    25429,
    // tab-technology-tools.
    25407,
    25409,
    25411,
    25413,
    25415,
    25417,
    25669,
  ];
  // Delete menu items.
  foreach ($menu_items as $menu_item) {
    $menu_link = \Drupal::entityTypeManager()->getStorage('menu_link_content')->load($menu_item);
    $menu_link->delete();
  }
}

/**
 * Update all nodes field_domain_access to oit_colorado_edu.
 */
function oit_update_10004() {
  \Drupal::state()->set('domain_update', 0);
}

/**
 * Remove Liaison key.
 */
function oit_update_10005() {
  $connection = \Drupal::database();
  $connection->delete('key_value')
    ->condition('name', 'liaison')
    ->execute();
}

/**
 * Truncate domain source table.
 */
function oit_update_10006() {
  \Drupal::database()->truncate('node__field_domain_source')->execute();
}

/**
 * Set domain source.
 */
function oit_update_10007() {
  \Drupal::database()->truncate('node__field_domain_source')->execute();

  // Sql query to 'node__field_domain_access' table where delta = 0 pulling in all columns.
  $query = \Drupal::database()->select('node__field_domain_access', 'nfa');
  $query->fields('nfa', [
    'bundle',
    'deleted',
    'entity_id',
    'revision_id',
    'langcode',
    'delta',
    'field_domain_access_target_id',
  ]);
  $query->condition('nfa.delta', 0);
  $result = $query->execute()->fetchAll();

  foreach ($result as $row) {
    $nid = $row->entity_id;
    $vid = $row->revision_id;
    $lang = $row->langcode;
    $bundle = $row->bundle;
    $target_id = $row->field_domain_access_target_id;
    $insert = \Drupal::database()->insert('node__field_domain_source')
      ->fields([
        'bundle',
        'deleted',
        'entity_id',
        'revision_id',
        'langcode',
        'delta',
        'field_domain_source_target_id',
      ])
      ->values([
        $bundle,
        0,
        $nid,
        $vid,
        $lang,
        0,
        $target_id,
      ])
      ->execute();
  }
}

/**
 * Track latest ban_ip id to update new ip's to teams.
 */
function oit_update_10008() {
  $query = \Drupal::database()->select('ban_ip', 'bi');
  $query->fields('bi', ['iid']);
  $query->orderBy('iid', 'DESC');
  $query->range(0, 1);
  $result = $query->execute()->fetchAll();
  $last_id = $result[0]->iid;
  \Drupal::state()->set('ban_ip_last_id', $last_id);
}

/**
 * Spring clean old tables.
 */
function oit_update_10009() {
  $tables = [
    '__ACQUIA_MONITORING',
    'cache_migrate',
    'migrate_map_block_content_body_field',
    'migrate_map_block_content_entity_display',
    'migrate_map_block_content_entity_form_display',
    'migrate_map_block_content_type',
    'migrate_map_contact_category',
    'migrate_map_d7_acquia_connector_settings',
    'migrate_map_d7_acquia_connector_subscription_data',
    'migrate_map_d7_block',
    'migrate_map_d7_comment',
    'migrate_map_d7_comment_entity_display',
    'migrate_map_d7_comment_entity_form_display',
    'migrate_map_d7_comment_entity_form_display_subject',
    'migrate_map_d7_comment_field',
    'migrate_map_d7_comment_field_instance',
    'migrate_map_d7_comment_type',
    'migrate_map_d7_contact_settings',
    'migrate_map_d7_custom_block',
    'migrate_map_d7_dblog_settings',
    'migrate_map_d7_field',
    'migrate_map_d7_field_formatter_settings',
    'migrate_map_d7_field_instance',
    'migrate_map_d7_field_instance_widget_settings',
    'migrate_map_d7_file',
    'migrate_map_d7_file_private',
    'migrate_map_d7_filter_format',
    'migrate_map_d7_filter_settings',
    'migrate_map_d7_global_theme_settings',
    'migrate_map_d7_image_settings',
    'migrate_map_d7_image_styles',
    'migrate_map_d7_menu',
    'migrate_map_d7_menu_links',
    'migrate_map_d7_metatag_field__node',
    'migrate_map_d7_metatag_field_instance__node__page',
    'migrate_map_d7_metatag_field_instance_widget_settings__node__pa',
    'migrate_map_d7_node_complete__news',
    'migrate_map_d7_node_complete__page',
    'migrate_map_d7_node_complete__service_alert',
    'migrate_map_d7_node_complete__tutorial',
    'migrate_map_d7_node_complete__webform',
    'migrate_map_d7_node_settings',
    'migrate_map_d7_node_title_label',
    'migrate_map_d7_node_type',
    'migrate_map_d7_path_redirect',
    'migrate_map_d7_pathauto_patterns',
    'migrate_map_d7_pathauto_settings',
    'migrate_map_d7_responsive_image_styles',
    'migrate_map_d7_search_page',
    'migrate_map_d7_shortcut',
    'migrate_map_d7_shortcut_set',
    'migrate_map_d7_shortcut_set_users',
    'migrate_map_d7_syslog_settings',
    'migrate_map_d7_system_authorize',
    'migrate_map_d7_system_cron',
    'migrate_map_d7_system_date',
    'migrate_map_d7_system_file',
    'migrate_map_d7_system_mail',
    'migrate_map_d7_system_performance',
    'migrate_map_d7_taxonomy_term__oit_categories',
    'migrate_map_d7_taxonomy_term__tac_control',
    'migrate_map_d7_taxonomy_term__vocabulary_11',
    'migrate_map_d7_taxonomy_vocabulary',
    'migrate_map_d7_theme_settings',
    'migrate_map_d7_url_alias',
    'migrate_map_d7_user',
    'migrate_map_d7_user_flood',
    'migrate_map_d7_user_mail',
    'migrate_map_d7_user_role',
    'migrate_map_d7_user_settings',
    'migrate_map_d7_view_modes',
    'migrate_map_file_settings',
    'migrate_map_menu_settings',
    'migrate_map_system_image',
    'migrate_map_system_image_gd',
    'migrate_map_system_logging',
    'migrate_map_system_maintenance',
    'migrate_map_system_rss',
    'migrate_map_system_site',
    'migrate_map_taxonomy_settings',
    'migrate_map_text_settings',
    'migrate_map_update_settings',
    'migrate_map_user_picture_entity_display',
    'migrate_map_user_picture_entity_form_display',
    'migrate_map_user_picture_field',
    'migrate_map_user_picture_field_instance',
    'migrate_message_block_content_body_field',
    'migrate_message_block_content_entity_display',
    'migrate_message_block_content_entity_form_display',
    'migrate_message_block_content_type',
    'migrate_message_contact_category',
    'migrate_message_d7_acquia_connector_settings',
    'migrate_message_d7_acquia_connector_subscription_data',
    'migrate_message_d7_block',
    'migrate_message_d7_comment',
    'migrate_message_d7_comment_entity_display',
    'migrate_message_d7_comment_entity_form_display',
    'migrate_message_d7_comment_entity_form_display_subject',
    'migrate_message_d7_comment_field',
    'migrate_message_d7_comment_field_instance',
    'migrate_message_d7_comment_type',
    'migrate_message_d7_contact_settings',
    'migrate_message_d7_custom_block',
    'migrate_message_d7_dblog_settings',
    'migrate_message_d7_field',
    'migrate_message_d7_field_formatter_settings',
    'migrate_message_d7_field_instance',
    'migrate_message_d7_field_instance_widget_settings',
    'migrate_message_d7_file',
    'migrate_message_d7_file_private',
    'migrate_message_d7_filter_format',
    'migrate_message_d7_filter_settings',
    'migrate_message_d7_global_theme_settings',
    'migrate_message_d7_image_settings',
    'migrate_message_d7_image_styles',
    'migrate_message_d7_menu',
    'migrate_message_d7_menu_links',
    'migrate_message_d7_metatag_field__node',
    'migrate_message_d7_metatag_field_instance__node__page',
    'migrate_message_d7_metatag_field_instance_widget_settings__node',
    'migrate_message_d7_node_complete__news',
    'migrate_message_d7_node_complete__page',
    'migrate_message_d7_node_complete__service_alert',
    'migrate_message_d7_node_complete__tutorial',
    'migrate_message_d7_node_complete__webform',
    'migrate_message_d7_node_settings',
    'migrate_message_d7_node_title_label',
    'migrate_message_d7_node_type',
    'migrate_message_d7_path_redirect',
    'migrate_message_d7_pathauto_patterns',
    'migrate_message_d7_pathauto_settings',
    'migrate_message_d7_responsive_image_styles',
    'migrate_message_d7_search_page',
    'migrate_message_d7_shortcut',
    'migrate_message_d7_shortcut_set',
    'migrate_message_d7_shortcut_set_users',
    'migrate_message_d7_syslog_settings',
    'migrate_message_d7_system_authorize',
    'migrate_message_d7_system_cron',
    'migrate_message_d7_system_date',
    'migrate_message_d7_system_file',
    'migrate_message_d7_system_mail',
    'migrate_message_d7_system_performance',
    'migrate_message_d7_taxonomy_term__oit_categories',
    'migrate_message_d7_taxonomy_term__tac_control',
    'migrate_message_d7_taxonomy_term__vocabulary_11',
    'migrate_message_d7_taxonomy_vocabulary',
    'migrate_message_d7_theme_settings',
    'migrate_message_d7_url_alias',
    'migrate_message_d7_user',
    'migrate_message_d7_user_flood',
    'migrate_message_d7_user_mail',
    'migrate_message_d7_user_role',
    'migrate_message_d7_user_settings',
    'migrate_message_d7_view_modes',
    'migrate_message_file_settings',
    'migrate_message_menu_settings',
    'migrate_message_system_image',
    'migrate_message_system_image_gd',
    'migrate_message_system_logging',
    'migrate_message_system_maintenance',
    'migrate_message_system_rss',
    'migrate_message_system_site',
    'migrate_message_taxonomy_settings',
    'migrate_message_text_settings',
    'migrate_message_update_settings',
    'migrate_message_user_picture_entity_display',
    'migrate_message_user_picture_entity_form_display',
    'migrate_message_user_picture_field',
    'migrate_message_user_picture_field_instance',
  ];
  foreach ($tables as $table) {
    \Drupal::database()->schema()->dropTable($table);
  }
}

/**
 * Ban ip array of questionable ip's.
 */
function oit_update_10010() {
  $biq = [];
  $biq = json_encode($biq);
  \Drupal::state()->set('ban_ip_questionable', $biq);
}

/**
 * Create redirect from /request-portal to /request-catalog.
 */
function oit_update_10011() {
  Redirect::create([
    'redirect_source' => 'request-portal',
    'redirect_redirect' => 'internal:/request-catalog',
    'language' => 'und',
    'status_code' => '301',
  ])->save();
}

/**
 * Delete old tutorials.
 */
function oit_update_10012() {
  $controller = \Drupal::entityTypeManager()->getStorage('node');
  $entities = $controller->loadMultiple([1560, 1561, 1563, 1566, 1568, 19726, 29301, 31301]);
  $controller->delete($entities);
}

/**
 * Create new oit_cron_run state.
 */
function oit_update_10013() {
  $cron_run = [
    'analytics' => 0,
    'autoban' => 0,
    'top_services' => 0,
    'top_tutorials' => 0,
    'archive_news' => 0,
    'archive_service_maintenance' => 0,
  ];
  $cron_run = json_encode($cron_run);
  \Drupal::state()->set('oit_cron_run', $cron_run);
}

/**
 * Remove promote from all news.
 */
function oit_update_10014() {
  // Entity query to get the most recent node type of 'news' with promote = 1.
  $node_storage = \Drupal::entityTypeManager()->getStorage('node');
  $query = $node_storage->getQuery();
  $query->condition('type', 'news');
  $query->condition('promote', 1);
  $query->accessCheck(FALSE);
  $query->sort('created', 'DESC');
  $nids = $query->execute();

  $n = 0;

  foreach ($nids as $nid) {
    if ($n != 0) {
      $node = $node_storage->load($nid);
      $node->set('promote', 0);
      $node->set('field_sympa_send', 0);
      $node->save();
    }
    $n++;
  }
}

/**
 * Revert schema.
 */
function oit_update_10015() {
  // Revert system schema version because of drupal downgrade.
  $connection = \Drupal::database();
  $connection->update('key_value')
    ->fields(['value' => 'i:10400;'])
    ->condition('collection', 'system.schema')
    ->condition('name', 'system')
    ->execute();

  $connection->delete('key_value')
    ->condition('collection', 'core.equivalent_updates')
    ->condition('name', 'system')
    ->execute();
}

/**
 * Delete state oit_cron_run.
 */
function oit_update_10016() {
  \Drupal::state()->delete('oit_cron_run');
}

/**
 * Create new menu 'services - conferencing services' and replicate menu items.
 */
function oit_update_10017() {
  $entity_type_manager = \Drupal::entityTypeManager();
  $menu_storage = $entity_type_manager->getStorage('menu');
  $menu_link_storage = $entity_type_manager->getStorage('menu_link_content');

  // Create new menu.
  $menu = $menu_storage->create([
    'id' => 'services-conferencing-services',
    'label' => 'Services - Conferencing Services',
    'description' => 'Menu for conferencing services',
  ]);
  $menu->save();

  // Map to track old ID -> new ID for parent relationships.
  $id_map = [];
  $total_count = 0;

  // Start with Conferencing Services menu item (ID: 3175).
  $root_id = 3175;

  // Recursive function to replicate menu items and their children.
  $replicate_menu_tree = function($source_id, $new_parent = '') use (&$replicate_menu_tree, $menu_link_storage, &$id_map, &$total_count) {
    $source_link = $menu_link_storage->load($source_id);

    if (!$source_link) {
      return;
    }

    // Create new menu link in the new menu.
    $new_link = MenuLinkContent::create([
      'title' => $source_link->getTitle(),
      'link' => $source_link->get('link')->getValue(),
      'menu_name' => 'services-conferencing-services',
      'weight' => $source_link->getWeight(),
      'enabled' => $source_link->isEnabled(),
      'expanded' => $source_link->isExpanded(),
      'parent' => $new_parent,
    ]);

    // Copy description if it exists.
    if ($source_link->hasField('description') && !$source_link->get('description')->isEmpty()) {
      $new_link->set('description', $source_link->get('description')->getValue());
    }

    $new_link->save();
    $new_id = $new_link->id();
    $new_uuid = $new_link->uuid();
    $id_map[$source_id] = $new_id;
    $total_count++;

    // Get UUID of source link to find its children.
    $source_uuid = $source_link->uuid();

    // Find all children of this menu item.
    $database = \Drupal::database();
    $query = $database->select('menu_link_content_data', 'mlcd');
    $query->fields('mlcd', ['id']);
    $query->condition('mlcd.parent', 'menu_link_content:' . $source_uuid);
    $query->orderBy('mlcd.weight');
    $query->orderBy('mlcd.id');
    $child_ids = $query->execute()->fetchCol();

    // Recursively replicate each child with UUID-based parent.
    foreach ($child_ids as $child_id) {
      $replicate_menu_tree($child_id, 'menu_link_content:' . $new_uuid);
    }
  };

  // Start replication from the root.
  $replicate_menu_tree($root_id);

  return t('Created menu "Services - Conferencing Services" and replicated @count menu items across all levels.', [
    '@count' => $total_count,
  ]);
}

/**
 * Create new menu 'Services - Consulting & Prof Services' and replicate menu items.
 */
function oit_update_10018() {
  $entity_type_manager = \Drupal::entityTypeManager();
  $menu_storage = $entity_type_manager->getStorage('menu');
  $menu_link_storage = $entity_type_manager->getStorage('menu_link_content');

  // Create new menu.
  $menu = $menu_storage->create([
    'id' => 'services-consulting-prof-svc',
    'label' => 'Services - Consulting & Prof Services',
    'description' => 'Menu for consulting and professional services',
  ]);
  $menu->save();

  // Map to track old ID -> new ID for parent relationships.
  $id_map = [];
  $total_count = 0;

  // Start with Consulting & Professional Services menu item (ID: 3179).
  $root_id = 3179;

  // Recursive function to replicate menu items and their children.
  $replicate_menu_tree = function($source_id, $new_parent = '') use (&$replicate_menu_tree, $menu_link_storage, &$id_map, &$total_count) {
    $source_link = $menu_link_storage->load($source_id);

    if (!$source_link) {
      return;
    }

    // Create new menu link in the new menu.
    $new_link = MenuLinkContent::create([
      'title' => $source_link->getTitle(),
      'link' => $source_link->get('link')->getValue(),
      'menu_name' => 'services-consulting-prof-svc',
      'weight' => $source_link->getWeight(),
      'enabled' => $source_link->isEnabled(),
      'expanded' => $source_link->isExpanded(),
      'parent' => $new_parent,
    ]);

    // Copy description if it exists.
    if ($source_link->hasField('description') && !$source_link->get('description')->isEmpty()) {
      $new_link->set('description', $source_link->get('description')->getValue());
    }

    $new_link->save();
    $new_id = $new_link->id();
    $new_uuid = $new_link->uuid();
    $id_map[$source_id] = $new_id;
    $total_count++;

    // Get UUID of source link to find its children.
    $source_uuid = $source_link->uuid();

    // Find all children of this menu item.
    $database = \Drupal::database();
    $query = $database->select('menu_link_content_data', 'mlcd');
    $query->fields('mlcd', ['id']);
    $query->condition('mlcd.parent', 'menu_link_content:' . $source_uuid);
    $query->orderBy('mlcd.weight');
    $query->orderBy('mlcd.id');
    $child_ids = $query->execute()->fetchCol();

    // Recursively replicate each child with UUID-based parent.
    foreach ($child_ids as $child_id) {
      $replicate_menu_tree($child_id, 'menu_link_content:' . $new_uuid);
    }
  };

  // Start replication from the root.
  $replicate_menu_tree($root_id);

  return t('Created menu "Services - Consulting & Prof Services" and replicated @count menu items across all levels.', [
    '@count' => $total_count,
  ]);
}

/**
 * Create new menu 'Services - Transfer, Storage, & Infrastructure' and replicate menu items.
 */
function oit_update_10019() {
  $entity_type_manager = \Drupal::entityTypeManager();
  $menu_storage = $entity_type_manager->getStorage('menu');
  $menu_link_storage = $entity_type_manager->getStorage('menu_link_content');

  // Create new menu.
  $menu = $menu_storage->create([
    'id' => 'services-transfer-storage',
    'label' => 'Services - Transfer, Storage, & Infrastructure',
    'description' => 'Menu for file transfer, storage and infrastructure services',
  ]);
  $menu->save();

  // Map to track old ID -> new ID for parent relationships.
  $id_map = [];
  $total_count = 0;

  // Start with File Transfer, Storage & Infrastructure menu item (ID: 3178).
  $root_id = 3178;

  // Recursive function to replicate menu items and their children.
  $replicate_menu_tree = function($source_id, $new_parent = '') use (&$replicate_menu_tree, $menu_link_storage, &$id_map, &$total_count) {
    $source_link = $menu_link_storage->load($source_id);

    if (!$source_link) {
      return;
    }

    // Create new menu link in the new menu.
    $new_link = MenuLinkContent::create([
      'title' => $source_link->getTitle(),
      'link' => $source_link->get('link')->getValue(),
      'menu_name' => 'services-transfer-storage',
      'weight' => $source_link->getWeight(),
      'enabled' => $source_link->isEnabled(),
      'expanded' => $source_link->isExpanded(),
      'parent' => $new_parent,
    ]);

    // Copy description if it exists.
    if ($source_link->hasField('description') && !$source_link->get('description')->isEmpty()) {
      $new_link->set('description', $source_link->get('description')->getValue());
    }

    $new_link->save();
    $new_id = $new_link->id();
    $new_uuid = $new_link->uuid();
    $id_map[$source_id] = $new_id;
    $total_count++;

    // Get UUID of source link to find its children.
    $source_uuid = $source_link->uuid();

    // Find all children of this menu item.
    $database = \Drupal::database();
    $query = $database->select('menu_link_content_data', 'mlcd');
    $query->fields('mlcd', ['id']);
    $query->condition('mlcd.parent', 'menu_link_content:' . $source_uuid);
    $query->orderBy('mlcd.weight');
    $query->orderBy('mlcd.id');
    $child_ids = $query->execute()->fetchCol();

    // Recursively replicate each child with UUID-based parent.
    foreach ($child_ids as $child_id) {
      $replicate_menu_tree($child_id, 'menu_link_content:' . $new_uuid);
    }
  };

  // Start replication from the root.
  $replicate_menu_tree($root_id);

  return t('Created menu "Services - Transfer, Storage, & Infrastructure" and replicated @count menu items across all levels.', [
    '@count' => $total_count,
  ]);
}

/**
 * Create new menu 'Services - IT Security' and replicate menu items.
 */
function oit_update_10020() {
  $entity_type_manager = \Drupal::entityTypeManager();
  $menu_storage = $entity_type_manager->getStorage('menu');
  $menu_link_storage = $entity_type_manager->getStorage('menu_link_content');

  // Create new menu.
  $menu = $menu_storage->create([
    'id' => 'services-it-security',
    'label' => 'Services - IT Security',
    'description' => 'Menu for IT security services',
  ]);
  $menu->save();

  // Map to track old ID -> new ID for parent relationships.
  $id_map = [];
  $total_count = 0;

  // Start with IT Security menu item (ID: 3598).
  $root_id = 3598;

  // Recursive function to replicate menu items and their children.
  $replicate_menu_tree = function($source_id, $new_parent = '') use (&$replicate_menu_tree, $menu_link_storage, &$id_map, &$total_count) {
    $source_link = $menu_link_storage->load($source_id);

    if (!$source_link) {
      return;
    }

    // Create new menu link in the new menu.
    $new_link = MenuLinkContent::create([
      'title' => $source_link->getTitle(),
      'link' => $source_link->get('link')->getValue(),
      'menu_name' => 'services-it-security',
      'weight' => $source_link->getWeight(),
      'enabled' => $source_link->isEnabled(),
      'expanded' => $source_link->isExpanded(),
      'parent' => $new_parent,
    ]);

    // Copy description if it exists.
    if ($source_link->hasField('description') && !$source_link->get('description')->isEmpty()) {
      $new_link->set('description', $source_link->get('description')->getValue());
    }

    $new_link->save();
    $new_id = $new_link->id();
    $new_uuid = $new_link->uuid();
    $id_map[$source_id] = $new_id;
    $total_count++;

    // Get UUID of source link to find its children.
    $source_uuid = $source_link->uuid();

    // Find all children of this menu item.
    $database = \Drupal::database();
    $query = $database->select('menu_link_content_data', 'mlcd');
    $query->fields('mlcd', ['id']);
    $query->condition('mlcd.parent', 'menu_link_content:' . $source_uuid);
    $query->orderBy('mlcd.weight');
    $query->orderBy('mlcd.id');
    $child_ids = $query->execute()->fetchCol();

    // Recursively replicate each child with UUID-based parent.
    foreach ($child_ids as $child_id) {
      $replicate_menu_tree($child_id, 'menu_link_content:' . $new_uuid);
    }
  };

  // Start replication from the root.
  $replicate_menu_tree($root_id);

  return t('Created menu "Services - IT Security" and replicated @count menu items across all levels.', [
    '@count' => $total_count,
  ]);
}

/**
 * Create new menu 'Services - Identity & Access Management' and replicate menu items.
 */
function oit_update_10021() {
  $entity_type_manager = \Drupal::entityTypeManager();
  $menu_storage = $entity_type_manager->getStorage('menu');
  $menu_link_storage = $entity_type_manager->getStorage('menu_link_content');

  // Create new menu.
  $menu = $menu_storage->create([
    'id' => 'services-identity-access',
    'label' => 'Services - Identity & Access Management',
    'description' => 'Menu for identity and access management services',
  ]);
  $menu->save();

  // Map to track old ID -> new ID for parent relationships.
  $id_map = [];
  $total_count = 0;

  // Start with Identity & Access Management menu item (ID: 9032).
  $root_id = 9032;

  // Recursive function to replicate menu items and their children.
  $replicate_menu_tree = function($source_id, $new_parent = '') use (&$replicate_menu_tree, $menu_link_storage, &$id_map, &$total_count) {
    $source_link = $menu_link_storage->load($source_id);

    if (!$source_link) {
      return;
    }

    // Create new menu link in the new menu.
    $new_link = MenuLinkContent::create([
      'title' => $source_link->getTitle(),
      'link' => $source_link->get('link')->getValue(),
      'menu_name' => 'services-identity-access',
      'weight' => $source_link->getWeight(),
      'enabled' => $source_link->isEnabled(),
      'expanded' => $source_link->isExpanded(),
      'parent' => $new_parent,
    ]);

    // Copy description if it exists.
    if ($source_link->hasField('description') && !$source_link->get('description')->isEmpty()) {
      $new_link->set('description', $source_link->get('description')->getValue());
    }

    $new_link->save();
    $new_id = $new_link->id();
    $new_uuid = $new_link->uuid();
    $id_map[$source_id] = $new_id;
    $total_count++;

    // Get UUID of source link to find its children.
    $source_uuid = $source_link->uuid();

    // Find all children of this menu item.
    $database = \Drupal::database();
    $query = $database->select('menu_link_content_data', 'mlcd');
    $query->fields('mlcd', ['id']);
    $query->condition('mlcd.parent', 'menu_link_content:' . $source_uuid);
    $query->orderBy('mlcd.weight');
    $query->orderBy('mlcd.id');
    $child_ids = $query->execute()->fetchCol();

    // Recursively replicate each child with UUID-based parent.
    foreach ($child_ids as $child_id) {
      $replicate_menu_tree($child_id, 'menu_link_content:' . $new_uuid);
    }
  };

  // Start replication from the root.
  $replicate_menu_tree($root_id);

  return t('Created menu "Services - Identity & Access Management" and replicated @count menu items across all levels.', [
    '@count' => $total_count,
  ]);
}

/**
 * Create new menu 'Services - Learning Spaces Technology' and replicate menu items.
 */
function oit_update_10022() {
  $entity_type_manager = \Drupal::entityTypeManager();
  $menu_storage = $entity_type_manager->getStorage('menu');
  $menu_link_storage = $entity_type_manager->getStorage('menu_link_content');

  // Create new menu.
  $menu = $menu_storage->create([
    'id' => 'services-learning-spaces',
    'label' => 'Services - Learning Spaces Technology',
    'description' => 'Menu for learning spaces technology services',
  ]);
  $menu->save();

  // Map to track old ID -> new ID for parent relationships.
  $id_map = [];
  $total_count = 0;

  // Start with Learning Spaces Technology menu item (ID: 3165).
  $root_id = 3165;

  // Recursive function to replicate menu items and their children.
  $replicate_menu_tree = function($source_id, $new_parent = '') use (&$replicate_menu_tree, $menu_link_storage, &$id_map, &$total_count) {
    $source_link = $menu_link_storage->load($source_id);

    if (!$source_link) {
      return;
    }

    // Create new menu link in the new menu.
    $new_link = MenuLinkContent::create([
      'title' => $source_link->getTitle(),
      'link' => $source_link->get('link')->getValue(),
      'menu_name' => 'services-learning-spaces',
      'weight' => $source_link->getWeight(),
      'enabled' => $source_link->isEnabled(),
      'expanded' => $source_link->isExpanded(),
      'parent' => $new_parent,
    ]);

    // Copy description if it exists.
    if ($source_link->hasField('description') && !$source_link->get('description')->isEmpty()) {
      $new_link->set('description', $source_link->get('description')->getValue());
    }

    $new_link->save();
    $new_id = $new_link->id();
    $new_uuid = $new_link->uuid();
    $id_map[$source_id] = $new_id;
    $total_count++;

    // Get UUID of source link to find its children.
    $source_uuid = $source_link->uuid();

    // Find all children of this menu item.
    $database = \Drupal::database();
    $query = $database->select('menu_link_content_data', 'mlcd');
    $query->fields('mlcd', ['id']);
    $query->condition('mlcd.parent', 'menu_link_content:' . $source_uuid);
    $query->orderBy('mlcd.weight');
    $query->orderBy('mlcd.id');
    $child_ids = $query->execute()->fetchCol();

    // Recursively replicate each child with UUID-based parent.
    foreach ($child_ids as $child_id) {
      $replicate_menu_tree($child_id, 'menu_link_content:' . $new_uuid);
    }
  };

  // Start replication from the root.
  $replicate_menu_tree($root_id);

  return t('Created menu "Services - Learning Spaces Technology" and replicated @count menu items across all levels.', [
    '@count' => $total_count,
  ]);
}

/**
 * Create new menu 'Services - Messaging & Collaboration' and replicate menu items.
 */
function oit_update_10023() {
  $entity_type_manager = \Drupal::entityTypeManager();
  $menu_storage = $entity_type_manager->getStorage('menu');
  $menu_link_storage = $entity_type_manager->getStorage('menu_link_content');

  // Create new menu.
  $menu = $menu_storage->create([
    'id' => 'services-messaging-collab',
    'label' => 'Services - Messaging & Collaboration',
    'description' => 'Menu for messaging and collaboration services',
  ]);
  $menu->save();

  // Map to track old ID -> new ID for parent relationships.
  $id_map = [];
  $total_count = 0;

  // Start with Messaging & Collaboration menu item (ID: 3542).
  $root_id = 3542;

  // Recursive function to replicate menu items and their children.
  $replicate_menu_tree = function($source_id, $new_parent = '') use (&$replicate_menu_tree, $menu_link_storage, &$id_map, &$total_count) {
    $source_link = $menu_link_storage->load($source_id);

    if (!$source_link) {
      return;
    }

    // Create new menu link in the new menu.
    $new_link = MenuLinkContent::create([
      'title' => $source_link->getTitle(),
      'link' => $source_link->get('link')->getValue(),
      'menu_name' => 'services-messaging-collab',
      'weight' => $source_link->getWeight(),
      'enabled' => $source_link->isEnabled(),
      'expanded' => $source_link->isExpanded(),
      'parent' => $new_parent,
    ]);

    // Copy description if it exists.
    if ($source_link->hasField('description') && !$source_link->get('description')->isEmpty()) {
      $new_link->set('description', $source_link->get('description')->getValue());
    }

    $new_link->save();
    $new_id = $new_link->id();
    $new_uuid = $new_link->uuid();
    $id_map[$source_id] = $new_id;
    $total_count++;

    // Get UUID of source link to find its children.
    $source_uuid = $source_link->uuid();

    // Find all children of this menu item.
    $database = \Drupal::database();
    $query = $database->select('menu_link_content_data', 'mlcd');
    $query->fields('mlcd', ['id']);
    $query->condition('mlcd.parent', 'menu_link_content:' . $source_uuid);
    $query->orderBy('mlcd.weight');
    $query->orderBy('mlcd.id');
    $child_ids = $query->execute()->fetchCol();

    // Recursively replicate each child with UUID-based parent.
    foreach ($child_ids as $child_id) {
      $replicate_menu_tree($child_id, 'menu_link_content:' . $new_uuid);
    }
  };

  // Start replication from the root.
  $replicate_menu_tree($root_id);

  return t('Created menu "Services - Messaging & Collaboration" and replicated @count menu items across all levels.', [
    '@count' => $total_count,
  ]);
}
